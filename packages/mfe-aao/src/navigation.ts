import type { NavigationGroup } from "@dash/mfe-types";

// AAO doesn't have its own navigation group, it's part of Counter Functions
// The navigation item for AAO is registered via the shell or rentals MFE
export const navigation: NavigationGroup[] = [];
