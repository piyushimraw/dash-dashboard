# External Integrations

**Analysis Date:** 2026-01-22

## APIs & External Services

**Third-Party Data APIs:**
- Dummy JSON API (`https://dummyjson.com/c/1394-326c-4220-88d7`) - Provides mock rental vehicle data
  - Used by: `src/features/rent-vehicle/api.ts`
  - SDK/Client: Native `fetch` API via custom `http` wrapper
  - Data format: JSON
  - Endpoint used: Vehicle rental list data

## Data Storage

**Databases:**
- Not applicable - No backend database integration detected

**Local Storage:**
- Browser LocalStorage only
  - Used for: PWA install banner dismissal state (`src/hooks/usePWAInstall.ts`)
  - Used for: Zustand auth state persistence (`src/store/useAuthStore.ts`)
  - Key: `auth-storage` for authentication state (user ID, role, login status)
  - Key: `PWA_INSTALL_DISMISSED` for PWA banner state

**Client:** Zustand with `persist` middleware for auth state management

**File Storage:**
- Not applicable - No cloud file storage or CDN integration

**Caching:**
- TanStack React Query
  - Default cache time: 5 minutes (`staleTime: 5 * 60 * 1000`)
  - Retry strategy: 1 retry on queries, 0 retries on mutations
  - `refetchOnWindowFocus: false` - Disabled auto-refetch when window regains focus
  - Configured in: `src/lib/react-query/queryClient.ts`

## Authentication & Identity

**Auth Provider:**
- Custom in-memory authentication
  - Implementation: Dummy user credentials stored in `src/config/users.ts`
  - No external auth provider (Supabase, Firebase, Auth0, etc.)
  - Login: Username and password validation against hardcoded DUMMY_USERS list
  - Stored in: Zustand store with localStorage persistence
  - State persisted to localStorage under key `auth-storage`

**User Roles:**
- Defined in `src/config/roles.ts`
- Role-based access control implemented in `src/auth/requireRole.ts`
- No external authorization service

## Monitoring & Observability

**Error Tracking:**
- Not detected - No error tracking service (Sentry, Rollbar, etc.)
- Basic error handling: Custom `http` wrapper catches and throws fetch errors

**Logs:**
- Browser console logging only
- No centralized logging service

**Analytics:**
- Not detected - No analytics service (Google Analytics, Mixpanel, etc.)

## CI/CD & Deployment

**Hosting:**
- Not configured - No deployment platform detected

**CI Pipeline:**
- Not configured - No CI/CD service detected (GitHub Actions, GitLab CI, etc.)

**Build Output:**
- Vite build target: `dist/` directory
- Build command: `npm run build` (TypeScript + Vite build)

## Environment Configuration

**Required env vars:**
- None - Application runs without environment variables

**Secrets location:**
- Not applicable - No external secrets or credentials required
- Dummy credentials in `src/config/users.ts` are for demo/testing only

## Webhooks & Callbacks

**Incoming:**
- Not applicable - No webhook receivers detected

**Outgoing:**
- Not applicable - No webhook senders detected

## PWA & Service Worker

**PWA Configuration:**
- Enabled via `vite-plugin-pwa`
- Service Worker: Auto-registered in `src/main.tsx`
- Auto-update: `registerType: "autoUpdate"`
- Dev mode: Service worker enabled during `vite dev`
- Manifest: Generated by plugin based on `vite.config.ts` configuration
- App name: "Hertz"
- Icons: PNG icons (192x192, 512x512) expected in `public/` directory
- Display: Standalone (full-screen app experience)
- Start URL: `/`
- Meta tags in `index.html` for iOS PWA support

## HTTP Client

**Custom HTTP Wrapper:**
- Location: `src/lib/api/http.ts`
- Implementation: Thin wrapper around native `fetch` API
- Features:
  - Automatic JSON content-type header
  - Error handling: Parses error responses as JSON and throws
  - Generic typing for response data
  - Used by all API calls in the application

**API Integration Points:**
- `src/features/rent-vehicle/api.ts` - Vehicle rental data fetching
- `src/features/rent-vehicle/query.ts` - React Query integration for rental data

## Form Handling & Validation

**Form Library:**
- React Hook Form 7.71.1 with Zod schema validation
- Resolver: @hookform/resolvers (Zod validation adapter)

**Validation Examples:**
- Login form: `src/forms/login/login.schema.ts`
- Rent vehicle form: `src/forms/rent/rent.schema.ts`
- Return vehicle form: `src/forms/return/return.schema.ts`

---

*Integration audit: 2026-01-22*
